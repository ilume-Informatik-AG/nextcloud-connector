# Copy to .env and adjust if needed: cp .env.example .env
# Matches the demo credentials baked into application-c8run.yml.

NEXTCLOUD_PORT=9000
NC_ADMIN=demo
NC_PASS=demo

DB_ROOT_PASSWORD=root
DB_PASSWORD=nextcloud
